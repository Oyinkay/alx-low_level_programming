#include <stdio.h>
/**
 * main - Entry point
 * Description: program to print numbers in base 10
 * Return: Always 0 (success)
*/
int main(void)
{
	int a
	for (a = 1 ; a < 10)
	printf("%d", a);
	return (0);
}
