#include <stdio.h>
/**
 * main - Entry point
 * Description: The program would write alphabets in lower and upper case
 * Return: Always return 0 (success)
*/

int main(void)

{
	char l
	char u

	for (i = 'a' ; i <= 'z' ; ++i; u = 'A' ; u <= 'Z' ; ++u)
	putchar(i, u);
	putchar('\n')
	return (0)
}
