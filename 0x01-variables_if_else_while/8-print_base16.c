#include <stdio.h>
/**
 * main - Entry point
 * Description: The program will print all hexadecimal number
 * Return: Always 0 (success)
*/

int main(void)
{
	int i;
	char a

	for (i = 0 ; i < 10 ; i++)
		putchar(i = '0');
	for (a = 'a' ; a <= 'f' a++)
		putchar(a);
	putchar('\n');
	return (0);
}
